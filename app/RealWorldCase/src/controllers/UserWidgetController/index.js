import AppDomianModel from '../../models/AppDomainModel';

export const initialize = ({ appName, match }) => (
  (dispatch, getState, context) => {
    const login = match.params.login.toLowerCase();
  }
);
export const dispose = ({ appName }) => (
  (dispatch, getState, context) => {

  }
);
